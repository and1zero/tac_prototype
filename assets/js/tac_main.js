/* 
 * TAC Prototype Main js class
 * 
 * Use this javascript only for initializing global functionality.
 * For example, if we want to initialize tooltip in every HTML element that
 * has rel="tooltip" as their attribute.
 */

$(document).ready(function() {
    init_page();
});